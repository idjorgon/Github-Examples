name: "Dep Jobs Example"

on: ['push']

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: stepB
        run: echo "Hello"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: stepA
        run: echo "World"